import React from 'react';
import { ChatInput, InputBox, SendButton } from './style';

const Input = () => (
  <ChatInput>
    <InputBox />
    <SendButton>send
    </SendButton>
  </ChatInput>
);

export default Input;
